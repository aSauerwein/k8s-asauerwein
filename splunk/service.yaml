apiVersion: v1
kind: Service
metadata:
  labels:
    app: splunk
  name: splunk
  namespace: splunk
spec:
  ports:
  - port: 8000
    name: http
    protocol: TCP
    targetPort: 8000
  selector:
    app: splunk
status:
  loadBalancer: {}
