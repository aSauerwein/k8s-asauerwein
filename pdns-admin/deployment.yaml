apiVersion: apps/v1
kind: Deployment
metadata:
  name: powerdns-admin
spec:
  selector:
    matchLabels:
      app: powerdns-admin
  replicas: 1
  template:
    metadata:
      labels:
        app: powerdns-admin
    spec:
      containers:
        - name: powerdns-admin
          image: ngoduykhanh/powerdns-admin:latest@sha256:111dbfba5be9eacc2cb7dd783e6b7ebbcdb688564503b66469261e6f9315b1b1
          env:
            - name: SQLALCHEMY_DATABASE_URI
              value: postgresql://ns001:12345ABCDEF4242@postgresql.pgsql.svc.cluster.local/ns001
            - name: BIND_ADDRESS
              value: "0.0.0.0:8000"
          securityContext:
            allowPrivilegeEscalation: false
          imagePullPolicy: IfNotPresent
          resources:
            limits:
              cpu: "250m"
              memory: "128Mi"
      restartPolicy: Always
      automountServiceAccountToken: false
